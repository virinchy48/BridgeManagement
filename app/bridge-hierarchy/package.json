{
  "name": "bridge-hierarchy",
  "version": "1.0.0",
  "description": "Bridge Hierarchy Explorer — standalone plug-and-play app",
  "scripts": {
    "start": "ui5 serve",
    "build": "ui5 build preload --clean-dest"
  },
  "devDependencies": {
    "@ui5/cli": "^3"
  }
}
