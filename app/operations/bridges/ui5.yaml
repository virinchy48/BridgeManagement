specVersion: '4.0'
metadata:
  name: bms.operations.bridges
type: application
resources:
  configuration:
    propertiesFileSourceEncoding: UTF-8
builder:
  resources:
    excludes:
      - "/test/**"
      - "/localService/**"
  customTasks:
    - name: ui5-task-zipper
      afterTask: generateVersionInfo
      configuration:
        archiveName: operations-bridges
        relativePaths: true
        additionalFiles:
          - xs-app.json
